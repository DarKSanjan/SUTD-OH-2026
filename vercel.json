{
  "buildCommand": "cd frontend && npm install && ./node_modules/.bin/tsc && ./node_modules/.bin/vite build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --prefix api",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
